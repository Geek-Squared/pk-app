rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, update, write,delete: if isSignedIn();
    }
  }
  
  match /databases/chapters/documents {
    match /{document=**} {
      allow update, write,delete: if !isClient();
    }
  }
  
  match /databases/posts/documents {
    match /{document=**} {
      allow update, write,delete: if !isClient();
    }
  }
  
  match /databases/{database}/users {
    match /{document=**} {
      allow read, update, write: if isSignedIn();
    }
  }
  
  //functions
  function isSignedIn(){
  return request.auth != null
  }
  
  function isClient(){
  return request.auth.token.client
  }
  
}